# 🚀 Space Between Art 完整部署指南
## Cloudflare Pages + Functions 整合版

---

## 架構說明

```
spacebetweenart.pages.dev
├── /                      → index.html（首頁）
├── /healing-universe      → healing-universe.html
├── /ai-chat              → ai-chat.html（AI 療癒小編）
├── /gallery              → gallery.html
├── /pharmacist/          → 逐光的藥師子站
├── /renovator/           → 違章靈魂改建事務所子站
└── /api/chat             → Pages Function（Claude API 代理）
         │
         ▼
    Claude API
```

**優點**：
- ✅ 網站和 API 同域名，無 CORS 問題
- ✅ 單一專案管理全部
- ✅ API Key 安全存放在環境變數
- ✅ 無限流量
- ✅ 全球 CDN 加速

---

## 第一部分：準備工作

### 1. 確認你有

- [ ] GitHub 帳號
- [ ] Cloudflare 帳號
- [ ] Anthropic API Key（[在此申請](https://console.anthropic.com/)）

### 2. 專案結構

```
space-between-art-cloudflare/
├── index.html                 # 首頁
├── healing-universe.html      # 療癒宇宙
├── ai-chat.html              # AI 療癒小編
├── gallery.html              # 畫廊
├── community.html            # 社群
├── shop.html                 # 商店
├── pharmacist/               # 逐光的藥師
│   ├── index.html
│   ├── characters.html
│   └── botanical-grimoire.html
├── renovator/                # 違章靈魂改建事務所
│   └── index.html
├── healing-universe/         # 療癒宇宙子頁面
│   ├── quiz.html            # 心靈建築測驗
│   └── plant-cards.html     # 療癒植物卡
├── functions/                # ⭐ Cloudflare Pages Functions
│   └── api/
│       └── chat.js          # Claude API 代理
└── docs/
    └── 部署指南.md
```

---

## 第二部分：上傳到 GitHub

### 方法 A：全新上傳

1. 在 GitHub 創建新 Repository
   - 名稱：`space-between-art`（或你喜歡的名稱）
   - 選擇 **Public** 或 **Private**
   - 不要勾選 README

2. 在本地解壓檔案並上傳

```bash
# 解壓檔案
unzip SpaceBetweenArt_Cloudflare_Complete.zip
cd space-between-art-cloudflare

# 初始化 Git
git init
git add .
git commit -m "初始化 Space Between Art 網站"

# 連接 GitHub（替換成你的 repo 網址）
git remote add origin https://github.com/你的帳號/space-between-art.git
git branch -M main
git push -u origin main
```

### 方法 B：更新現有 Repository

```bash
# 進入現有專案資料夾
cd your-existing-repo

# 複製新檔案覆蓋舊檔案
# （手動或用檔案管理器）

# 提交更新
git add .
git commit -m "升級至 Cloudflare Pages + Functions"
git push origin main
```

---

## 第三部分：連接 Cloudflare Pages

### 步驟 1：進入 Cloudflare

1. 登入 https://dash.cloudflare.com
2. 左側選單點擊 **「Workers & Pages」**
3. 點擊 **「Create」**
4. 選擇 **「Pages」** 標籤

### 步驟 2：連接 GitHub

1. 點擊 **「Connect to Git」**
2. 選擇 **GitHub**
3. 授權 Cloudflare 訪問你的 GitHub
4. 選擇 `space-between-art` repository

### 步驟 3：配置建置設定

| 設定項目 | 填入值 |
|---------|-------|
| Project name | `spacebetweenart`（會變成 spacebetweenart.pages.dev） |
| Production branch | `main` |
| Framework preset | **None** |
| Build command | **留空** |
| Build output directory | **留空** 或 `/` |

### 步驟 4：先部署（不含 API Key）

1. 點擊 **「Save and Deploy」**
2. 等待 1-2 分鐘
3. 完成後會得到網址：`spacebetweenart.pages.dev`

⚠️ 此時 AI 小編功能還不能用，因為還沒設定 API Key

---

## 第四部分：設定 API Key（重要！）

### 步驟 1：進入專案設定

1. 在 Cloudflare Dashboard 點擊你的 Pages 專案
2. 點擊上方的 **「Settings」** 標籤
3. 左側選單點擊 **「Environment variables」**

### 步驟 2：添加 API Key

1. 點擊 **「Add variable」**
2. 填入：
   - **Variable name**: `CLAUDE_API_KEY`
   - **Value**: 你的 Anthropic API Key（sk-ant-api...）
3. 選擇環境：**Production** 和 **Preview** 都要勾選
4. 點擊 **「Save」**

### 步驟 3：重新部署

環境變數設定後需要重新部署才會生效：

1. 點擊 **「Deployments」** 標籤
2. 找到最新的部署
3. 點擊右側的 **「⋮」** → **「Retry deployment」**

或者在 GitHub 做任意修改並 push，會自動觸發重新部署。

---

## 第五部分：測試

### 測試網站

1. 訪問 `https://spacebetweenart.pages.dev`
2. 確認所有頁面正常顯示
3. 確認導航連結正確

### 測試 AI 小編

1. 訪問 `https://spacebetweenart.pages.dev/ai-chat.html`
2. 選擇一個角色
3. 發送訊息測試
4. 確認能收到回覆

### 測試 API 端點

在瀏覽器開發者工具的 Console 中執行：

```javascript
fetch('/api/chat', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        message: '你好',
        character: 'xi'
    })
}).then(r => r.json()).then(console.log)
```

應該看到類似：

```json
{
    "success": true,
    "reply": "孩子，你好啊。來，坐下喝杯茶...",
    "character": "xi"
}
```

---

## 第六部分：自訂域名（選用）

### 如果你有自己的域名

1. 在 Pages 專案頁面，點擊 **「Custom domains」**
2. 點擊 **「Set up a custom domain」**
3. 輸入你的域名（例如 `spacebetweenart.com`）
4. 按照指示設定 DNS

### DNS 設定

在你的域名商後台添加：

| 類型 | 名稱 | 值 |
|-----|-----|---|
| CNAME | @ | `spacebetweenart.pages.dev` |
| CNAME | www | `spacebetweenart.pages.dev` |

---

## 第七部分：日後更新

### 更新網站內容

```bash
# 修改檔案後
git add .
git commit -m "更新內容"
git push origin main

# Cloudflare 會自動偵測並重新部署（約 30-60 秒）
```

### 更新 API Key

1. 進入 Cloudflare Dashboard → 你的專案
2. Settings → Environment variables
3. 編輯 `CLAUDE_API_KEY`
4. 重新部署

### 查看部署歷史

在 Cloudflare Dashboard → Deployments 可以：
- 查看所有部署記錄
- 查看部署日誌
- 回滾到之前的版本

---

## 常見問題

### Q1：AI 小編回覆「API Key 未設定」

**解決**：
1. 確認環境變數名稱是 `CLAUDE_API_KEY`（全大寫、底線）
2. 確認 Production 環境有勾選
3. 重新部署

### Q2：AI 小編回覆「連線發生錯誤」

**可能原因**：
- API Key 無效或過期
- 帳戶餘額不足
- Claude API 暫時不可用

**排查**：
1. 檢查 Anthropic Console 的 API Key 狀態
2. 檢查帳戶餘額
3. 在 Cloudflare Functions 日誌查看錯誤

### Q3：Functions 日誌在哪裡看？

1. 進入專案頁面
2. 點擊某次部署
3. 點擊 **「Functions」** 標籤
4. 可以看到即時日誌

### Q4：如何測試不同分支？

Cloudflare Pages 會為每個分支創建預覽：
- `main` → `spacebetweenart.pages.dev`
- `dev` → `dev.spacebetweenart.pages.dev`

---

## 費用說明

### Cloudflare Pages（免費）

- 無限網站數量
- 無限流量
- 自動 SSL
- 全球 CDN

### Cloudflare Functions（免費額度）

| 項目 | 免費額度 |
|------|---------|
| 每日請求數 | 100,000 次 |
| CPU 時間 | 10ms/請求 |

**估算**：假設每天 100 人使用 AI 小編，每人對話 10 次 = 1,000 次/天，遠低於免費額度。

### Claude API（按量計費）

| 模型 | 輸入 | 輸出 |
|------|------|------|
| Claude Sonnet 4 | $3/百萬 token | $15/百萬 token |

**估算**：假設每次對話約 500 token，每天 1,000 次對話 = 500,000 token ≈ $2-3/天

---

## 檢查清單

### 部署前

- [ ] GitHub Repository 已創建
- [ ] 所有檔案已上傳
- [ ] `functions/api/chat.js` 存在

### 部署後

- [ ] 網站可訪問
- [ ] 所有頁面正常
- [ ] 環境變數已設定
- [ ] AI 小編功能正常
- [ ] 自訂域名設定（如有）

### 測試

- [ ] 首頁載入正常
- [ ] 療癒宇宙頁面正常
- [ ] AI 小編四個角色都能對話
- [ ] 心靈建築測驗正常
- [ ] 手機版顯示正常

---

## 支援資源

- **Cloudflare Pages 文件**：https://developers.cloudflare.com/pages/
- **Pages Functions 文件**：https://developers.cloudflare.com/pages/functions/
- **Claude API 文件**：https://docs.anthropic.com/
- **GitHub Pages 遷移指南**：https://developers.cloudflare.com/pages/migrations/

---

*Space Between Art | 部署文件 v1.0*
